const { BaseController } = require(".");

class TaskController extends BaseController {
    async exec (req, res) => {
        try {
            const { Task } = model;
            const {  }  
        } catch (err) {
            return 
        }
    }
};
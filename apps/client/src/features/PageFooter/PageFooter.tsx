import { Layout } from "@consta/uikit/Layout";
import styles from "./PageFooter.module.scss";

export const PageFooter = () => {
	return (<Layout className={styles["page-footer"]}>
			<div>Footer</div>
		</Layout>)
}